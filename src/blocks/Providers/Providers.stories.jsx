import React from 'react';

import { Providers } from './Providers';

export default {
  title: 'Country Admin UI/blocks/Providers',
  component: Providers,
  argTypes: {},
};

const Template = (props) => <Providers {...props} />;

export const Default = Template.bind({});
Default.args = {};
